<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "mybatis-3-mapper.dtd" >
<mapper namespace="me._hanho.nextjs_shop.buy.BuyMapper">

 	<!-- 전역 가용수량: stock - sum(HOLD & 미만료) -->
	<select id="selectAvailability" resultType="me._hanho.nextjs_shop.buy.AvailabilityRow">
		SELECT
			po.product_option_id,
			GREATEST(
			po.stock - IFNULL(SUM(
				CASE
				WHEN sh.status = 'HOLD'
				AND sh.active_hold = 1
				AND sh.expires_at > NOW()
				THEN sh.count
				ELSE 0
				END
			), 0),
			0
			) AS available
		FROM nextjs_shop_product_option po
		JOIN nextjs_shop_product p ON p.product_id = po.product_id
		JOIN nextjs_shop_seller s ON s.seller_no = p.seller_no 
		LEFT JOIN nextjs_shop_stock_hold sh
			ON sh.product_option_id = po.product_option_id
		WHERE s.approval_status = 'APPROVED' AND p.sale_stop = 0 AND
			po.product_option_id IN
			<foreach collection="detailIds" item="id" open="(" separator="," close=")">
			#{id}
			</foreach>
		GROUP BY po.product_option_id
	</select>

  <!-- 본인 미만료 HOLD -->
  <select id="selectExistingHolds" resultType="me._hanho.nextjs_shop.buy.ExistingHold">
    SELECT hold_id, product_option_id, count
    FROM nextjs_shop_stock_hold
    WHERE user_no = #{userNo}
      AND status = 'HOLD'
      AND expires_at > NOW()
      AND product_option_id IN
        <foreach collection="detailIds" item="id" open="(" separator="," close=")">
          #{id}
        </foreach>
  </select>

	<!-- 멱등 업서트: 유니크(UQ_user_pd_status) 충돌 시 count/TTL 갱신 -->
	<!-- 판매가능 조건을 통과한 옵션만 upsert -->
	<insert id="upsertHolds" parameterType="map">
		INSERT INTO nextjs_shop_stock_hold
			(user_no, product_option_id, cart_id, count, status, active_hold, expires_at, created_at, return_url)
		SELECT
			x.user_no,
			x.product_option_id,
			x.cart_id,
			x.count,
			'HOLD',
			1,
			DATE_ADD(NOW(), INTERVAL x.ttl_seconds SECOND),
			NOW(),
			x.return_url
		FROM (
			<foreach collection="rows" item="r" separator=" UNION ALL ">
			SELECT
				#{r.userNo}          AS user_no,
				#{r.productOptionId} AS product_option_id,
				#{r.cartId}          AS cart_id,
				#{r.count}           AS count,
				#{r.ttlSeconds}      AS ttl_seconds,
				#{r.returnUrl}       AS return_url
			</foreach>
		) x
		JOIN nextjs_shop_product_option po ON po.product_option_id = x.product_option_id
		JOIN nextjs_shop_product p ON p.product_id = po.product_id
		JOIN nextjs_shop_seller s ON s.seller_no = p.seller_no
		WHERE s.approval_status = 'APPROVED'
			AND p.sale_stop = 0
		ON DUPLICATE KEY UPDATE
			cart_id     = VALUES(cart_id),
			count       = VALUES(count),
			expires_at  = VALUES(expires_at),
			status      = 'HOLD',
			active_hold = 1
	</insert>

	<!-- ↑ MySQL에서 VALUES(expires_at) 사용 불가 이슈 회피:
		expires_at를 직접 다시 계산해서 갱신 -->
	<insert id="upsertHolds" databaseId="mysql">
		INSERT INTO nextjs_shop_stock_hold
			(user_no, product_option_id, cart_id, count, status, active_hold, expires_at, created_at, return_url)
		VALUES
			<foreach collection="rows" item="r" separator=",">
			(
				#{r.userNo},
				#{r.productOptionId},
				#{r.cartId},
				#{r.count},
				'HOLD',
				1,
				DATE_ADD(NOW(), INTERVAL #{r.ttlSeconds} SECOND),
				NOW(),
				#{r.returnUrl}
			)
			</foreach>
		ON DUPLICATE KEY UPDATE
			cart_id = VALUES(cart_id),
			count = VALUES(count),
			active_hold = 1,
			status = 'HOLD',
			expires_at = DATE_ADD(NOW(), INTERVAL
			<choose>
				<when test="rows != null and rows.size() == 1">
				#{rows[0].ttlSeconds}
				</when>
				<otherwise>
				180
				</otherwise>
			</choose>
			SECOND)
	</insert>

  <!-- 최신 HOLD (유저+pdId별 1건) -->
  <select id="selectLatestHolds" resultType="me._hanho.nextjs_shop.buy.HoldBrief">
    SELECT t.product_option_id, t.hold_id, t.count
    FROM nextjs_shop_stock_hold t
    INNER JOIN (
      SELECT product_option_id, MAX(hold_id) AS max_id
      FROM nextjs_shop_stock_hold
      WHERE user_no = #{userNo}
      	AND status = 'HOLD'
		AND active_hold = 1
		AND expires_at > NOW()
        AND product_option_id IN
          <foreach collection="detailIds" item="id" open="(" separator="," close=")">
            #{id}
          </foreach>
        AND status = 'HOLD'
      GROUP BY product_option_id
    ) m ON m.max_id = t.hold_id
  </select>

	<!-- nextjs_shop_user_coupon에 없는 쿠폰만 다운로드(insert) -->
	<insert id="downloadCoupons" parameterType="map">
		INSERT IGNORE INTO nextjs_shop_user_coupon (user_no, coupon_id)
		SELECT #{userNo}, c.coupon_id
		FROM nextjs_shop_coupon c
		WHERE c.status = 'ACTIVE'
		AND c.coupon_id IN
		<foreach collection="couponIds" item="couponId" open="(" separator="," close=")">
			#{couponId}
		</foreach>
		<!-- AND NOW() BETWEEN c.start_date AND c.end_date 포폴용 주석 -->
	</insert>

	<select id="selectUserCouponsByCouponIds" resultType="me._hanho.nextjs_shop.buy.UserCouponRow">
		SELECT uc.user_coupon_id, uc.coupon_id
		FROM nextjs_shop_user_coupon uc
		WHERE uc.user_no = #{userNo}
		AND uc.coupon_id IN
		<foreach collection="couponIds" item="couponId" open="(" separator="," close=")">
			#{couponId}
		</foreach>
	</select>

	<delete id="deleteHoldCouponsByHoldIds">
		DELETE FROM nextjs_shop_stock_hold_coupon
		WHERE hold_id IN
		<foreach collection="holdIds" item="holdId" open="(" separator="," close=")">
			#{holdId}
		</foreach>
	</delete>

	<insert id="applyCoupons" parameterType="list">
		INSERT INTO nextjs_shop_stock_hold_coupon (hold_id, user_coupon_id)
		VALUES
		<foreach collection="holdCoupons" item="ac" separator=",">
			(#{ac.holdId}, #{ac.userCouponId})
		</foreach>
	</insert>

	<!-- ================================= -->

	<select id="selectAllActiveHoldsByUser">
		SELECT sh.hold_id
		FROM nextjs_shop_stock_hold sh
		WHERE sh.user_no = #{userNo} AND sh.status = 'HOLD' AND sh.active_hold = 1
	</select>
	
	<!-- 점유 연장: 활성(HOLD & active_hold=1) 인 것만 NOW()+TTL 로 갱신 -->
	<update id="extendHolds">
	  UPDATE nextjs_shop_stock_hold sh
	  JOIN nextjs_shop_product_option po ON po.product_option_id = sh.product_option_id
	  JOIN nextjs_shop_product p ON p.product_id = po.product_id
	  JOIN nextjs_shop_seller s ON s.seller_no = p.seller_no
	  SET sh.expires_at = DATE_ADD(NOW(), INTERVAL #{ttlSeconds} SECOND)
	  WHERE sh.user_no = #{userNo}
	    AND sh.status = 'HOLD'
	    AND sh.active_hold = 1
	    AND sh.expires_at > NOW()
	    AND s.approval_status = 'APPROVED'
	    AND p.sale_stop = 0
	    AND sh.hold_id IN
	    <foreach collection="holdIds" item="id" open="(" separator="," close=")">
	      #{id}
	    </foreach>
	</update>

	<!-- 점유 해제: 활성(HOLD & active_hold=1) → RELEASED, active_hold=NULL -->
	<update id="releaseHolds">
	    UPDATE nextjs_shop_stock_hold
	    SET status = 'RELEASED',
	        active_hold = 0
	    WHERE user_no = #{userNo} AND status = 'HOLD'
			AND active_hold = 1
			AND hold_id IN
			<foreach collection="holdIds" item="id" open="(" separator="," close=")">
			  #{id}
			</foreach>
 	</update>

	<!-- 점유쿠폰 추가 -->
	<insert id="addStockHoldCoupons" parameterType="map">
		INSERT INTO nextjs_shop_stock_hold_coupon (hold_id, user_coupon_id)
		VALUES
		<foreach collection="holdCouponRequests" item="request" separator=",">
			(#{request.holdId}, #{request.userCouponId})
		</foreach>
	</insert>

	<!-- 점유쿠폰 제거 -->
	<delete id="deleteStockHoldCoupons">
		DELETE FROM nextjs_shop_stock_hold_coupon
		WHERE (hold_id, user_coupon_id) IN
		<foreach collection="holdCouponRequests" item="request" open="(" separator="," close=")">
			(#{request.holdId}, #{request.userCouponId})
		</foreach>
	</delete>

   	<!-- 점유하고 있는 상품조회 -->
  	<select id="getStockHoldProductList">
		SELECT sh.hold_id, sh.count, 
			po.product_option_id, po.add_price, po.size,
			p.product_id, p.name AS productName, p.origin_price, p.final_price, p.color_name,
			w.wish_id,
			s.seller_name, s.base_shipping_fee, s.free_shipping_min_amount, s.extra_shipping_fee,
			f.file_name, f.store_name, f.file_path, f.copyright, f.copyright_url
		FROM nextjs_shop_stock_hold sh
		JOIN nextjs_shop_product_option po ON po.product_option_id = sh.product_option_id
		JOIN nextjs_shop_product p ON p.product_id = po.product_id
		LEFT JOIN nextjs_shop_wish w ON w.product_id = p.product_id AND w.user_no = sh.user_no
		JOIN nextjs_shop_seller s ON s.seller_no = p.seller_no
		LEFT JOIN (
			SELECT 
				pi.product_id, 
				pi.file_id
			FROM nextjs_shop_product_image pi
			INNER JOIN (
				SELECT 
					product_id, 
					MIN(sort_key) AS min_sort_key
				FROM nextjs_shop_product_image
				GROUP BY product_id
			) min_pi 
			ON pi.product_id = min_pi.product_id 
			AND pi.sort_key = min_pi.min_sort_key
		) tpi ON tpi.product_id = p.product_id
		LEFT JOIN nextjs_shop_file f ON f.file_id = tpi.file_id
		WHERE sh.user_no = #{userNo} 
		AND sh.status = 'HOLD'
		AND sh.active_hold = 1
		AND sh.expires_at > NOW()
		AND s.approval_status = 'APPROVED'
		AND po.stock > 0
		AND po.is_displayed = 1
		AND p.sale_stop = 0
		ORDER BY sh.created_at DESC
	</select>

	<!-- 최신 생성일 기준 점유 정보 조회 -->
	<select id="getMaxCreatedAtStockHold">
		SELECT MAX(created_at)
		FROM nextjs_shop_stock_hold
		WHERE user_no = #{userNo}
	</select>

	<!-- 최신 생성일 기준 점유 정보 조회 -->
	<select id="getLatestHoldsInfo">
		SELECT sh.hold_id, sh.return_url, sh.status, sh.active_hold, sh.expires_at, 
			po.stock, po.is_displayed,
			p.product_id, p.sale_stop, 
			s.approval_status
		FROM nextjs_shop_stock_hold sh
		JOIN nextjs_shop_product_option po ON po.product_option_id = sh.product_option_id
		JOIN nextjs_shop_product p ON p.product_id = po.product_id
		JOIN nextjs_shop_seller s ON s.seller_no = p.seller_no
		WHERE sh.user_no = #{userNo}
		AND sh.created_at = #{latestCreatedAt}
		ORDER BY sh.hold_id DESC
	</select>

	<!-- 점유상품에 사용가능한 장바구니 쿠폰조회 -->
	<select id="getAvailableCartCouponsAtBuy">
		SELECT 
			uc.user_coupon_id, 
			c.coupon_id, c.description, c.discount_type, c.discount_value, 
			c.max_discount, c.minimum_order_before_amount,
			c.is_stackable, c.is_product_restricted,
			c.amount, c.start_date, c.end_date, c.issue_method,
			ca.coupon_allowed_id, ca.product_id
		FROM nextjs_shop_user_coupon uc
		JOIN nextjs_shop_coupon c ON uc.coupon_id = c.coupon_id
		LEFT JOIN nextjs_shop_coupon_allowed ca ON ca.coupon_id = c.coupon_id
		AND ca.product_id IN
			<foreach collection="productIds" item="productId" open="(" separator="," close=")">
				#{productId}
			</foreach>
		WHERE 
			uc.user_no = #{userNo} AND uc.used = 0
			AND c.seller_no IS NULL AND c.admin_no IS NOT NULL
			AND c.is_deleted = 0 AND c.status = 'ACTIVE' AND c.amount > 0
			AND (
				c.is_product_restricted = 0
				OR ca.product_id IS NOT NULL
			)
			AND (
				c.issue_method = 'CLAIM'
				OR (c.issue_method IN ('MANUAL','AUTO') AND uc.user_coupon_id IS NOT NULL)
			)
			<!-- AND NOW() BETWEEN c.start_date AND c.end_date 포폴용 주석 -->
	</select>
  
	<!-- 점유상품에 사용가능한 판매자 쿠폰조회 -->
	<select id="getAvailableSellerCouponsAtBuy">
		SELECT 
			uc.user_coupon_id, 
			c.coupon_id, c.description, c.discount_type, c.discount_value, 
			c.max_discount, c.minimum_order_before_amount,
			c.is_stackable, c.is_product_restricted,
			c.amount, c.start_date, c.end_date, c.issue_method,
			ca.coupon_allowed_id, ca.product_id,
			s.seller_name
		FROM nextjs_shop_user_coupon uc
		JOIN nextjs_shop_coupon c ON uc.coupon_id = c.coupon_id AND uc.used = 0
		JOIN nextjs_shop_product p ON c.seller_no = p.seller_no
			AND p.product_id IN
			<foreach collection="productIds" item="productId" open="(" separator="," close=")">
				#{productId}
			</foreach>
		LEFT JOIN nextjs_shop_coupon_allowed ca 
			ON ca.coupon_id = c.coupon_id AND ca.product_id = p.product_id
		JOIN nextjs_shop_seller s ON c.seller_no = s.seller_no
		WHERE 
			uc.user_no = #{userNo} 
			AND c.is_deleted = 0 AND c.status = 'ACTIVE' AND c.amount > 0
			AND (
				c.is_product_restricted = 0
				OR ca.product_id IS NOT NULL
			)
			AND (
				c.issue_method = 'CLAIM'
				OR (c.issue_method IN ('MANUAL','AUTO') AND uc.user_coupon_id IS NOT NULL)
			)
			<!-- AND NOW() BETWEEN c.start_date AND c.end_date 포폴용 주석 -->
	</select>

	<select id="selectHoldCouponsByHoldIds">
		SELECT *
		FROM nextjs_shop_stock_hold_coupon
		WHERE hold_id IN
			<foreach collection="holdIds" item="holdId" open="(" separator="," close=")">
				#{holdId}	
			</foreach>
	</select>

	<select id="getDefaultAddress">
		SELECT address_id, address_name, recipient_name, address_phone,
		 zonecode, address, address_detail, memo, default_address
		FROM nextjs_shop_user_address
		WHERE user_no = #{userNo} AND default_address = 1
	</select>
  
   
  
  <!-- <==== 결제 ================================================================> -->
  
  	<!-- 점유하고 있는 상품조회 -->
  	<select id="getStockHoldProductListByHoldIds">
		SELECT sh.hold_id, sh.count, 
			po.product_option_id, po.add_price, po.size,
			p.product_id, p.name AS productName, p.origin_price, p.final_price, p.color_name,
			w.wish_id,
			s.seller_name, s.base_shipping_fee, s.free_shipping_min_amount, s.extra_shipping_fee,
			f.file_name, f.store_name, f.file_path, f.copyright, f.copyright_url
		FROM nextjs_shop_stock_hold sh
		JOIN nextjs_shop_product_option po ON po.product_option_id = sh.product_option_id
		JOIN nextjs_shop_product p ON p.product_id = po.product_id
		LEFT JOIN nextjs_shop_wish w ON w.product_id = p.product_id AND w.user_no = sh.user_no
		JOIN nextjs_shop_seller s ON s.seller_no = p.seller_no
		LEFT JOIN (
			SELECT 
				pi.product_id, 
				pi.file_id
			FROM nextjs_shop_product_image pi
			INNER JOIN (
				SELECT 
					product_id, 
					MIN(sort_key) AS min_sort_key
				FROM nextjs_shop_product_image
				GROUP BY product_id
			) min_pi 
			ON pi.product_id = min_pi.product_id 
			AND pi.sort_key = min_pi.min_sort_key
		) tpi ON tpi.product_id = p.product_id
		LEFT JOIN nextjs_shop_file f ON f.file_id = tpi.file_id
		WHERE sh.user_no = #{userNo} 
		AND sh.hold_id IN
			<foreach collection="holdIds" item="holdId" open="(" separator="," close=")">
				#{holdId}
			</foreach>
		AND sh.status = 'HOLD'
		AND sh.active_hold = 1
		AND sh.expires_at > NOW()
		AND s.approval_status = 'APPROVED'
		AND po.stock > 0
		AND po.is_displayed = 1
		AND p.sale_stop = 0
		ORDER BY sh.created_at DESC
	</select>

	<select id="getAvailableCouponsByHoldIds">
		SELECT 
			shc.hold_coupon_id, shc.hold_id, 
			uc.user_coupon_id, 
			c.coupon_id, c.description, c.discount_type, c.discount_value, 
			c.max_discount, c.minimum_order_before_amount,
			c.is_stackable, c.is_product_restricted,
			c.amount, c.start_date, c.end_date, c.issue_method,
			ca.coupon_allowed_id, ca.product_id,
			s.seller_name
		FROM nextjs_shop_stock_hold_coupon shc
		JOIN nextjs_shop_stock_hold sh ON sh.hold_id = shc.hold_id AND sh.status = 'HOLD' AND sh.active_hold = 1 AND sh.expires_at > NOW()
		JOIN nextjs_shop_product_option po ON po.product_option_id = sh.product_option_id
		JOIN nextjs_shop_product p ON po.product_id = p.product_id
		JOIN nextjs_shop_user_coupon uc ON shc.user_coupon_id = uc.user_coupon_id AND uc.used = 0
		JOIN nextjs_shop_coupon c ON uc.coupon_id = c.coupon_id AND uc.used = 0
		LEFT JOIN nextjs_shop_coupon_allowed ca 
			ON ca.coupon_id = c.coupon_id AND ca.product_id = p.product_id
		LEFT JOIN nextjs_shop_seller s ON c.seller_no = s.seller_no
		WHERE shc.hold_id IN
				<foreach collection="holdIds" item="holdId" open="(" separator="," close=")">
					#{holdId}
				</foreach>
			AND c.is_deleted = 0 AND c.status = 'ACTIVE' AND c.amount > 0
			AND (
				c.is_product_restricted = 0
				OR ca.product_id IS NOT NULL
			)
			AND (
				c.issue_method = 'CLAIM'
				OR (c.issue_method IN ('MANUAL','AUTO') AND uc.user_coupon_id IS NOT NULL)
			)
			<!-- AND NOW() BETWEEN c.start_date AND c.end_date 포폴용 주석 -->
	</select>

</mapper>
